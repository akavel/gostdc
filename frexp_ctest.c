
#define gostdc_stdlib_h
#include "ctest.h"

//FIXME
void
TestFrexp(void) {
/*
    double r;
    int exp;

    r = frexp(1.0, &exp);
    if (r*((double)(2<<exp)) != 1.0)
    //if (r!=0.625 || exp!=4)
        runtime·panicstring("frexp failed 1");
*/
}

